name: Release Build

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Version number (e.g. 1.2.3)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Validate version format
        run: |
          if [[ ! "${{ inputs.version }}" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Version must look like 0.0.0 (got '${{ inputs.version }}')"
            exit 1
          fi

  build:
    needs: validate
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: macos-latest
            platform: mac
          - os: ubuntu-latest
            platform: linux
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-go@v5
        with:
          go-version: "1.25"

      - uses: pnpm/action-setup@v4
        with:
          # pinned to match the pnpm version the lockfile (patchedDependencies
          # hashes especially) was generated with — a major-version mismatch
          # makes `pnpm install --frozen-lockfile` fail with
          # ERR_PNPM_LOCKFILE_CONFIG_MISMATCH even though nothing is actually stale
          version: 10.33.2

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "pnpm"
          cache-dependency-path: frontend/pnpm-lock.yaml

      - name: Install Linux system deps
        if: matrix.platform == 'linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.1-dev build-essential pkg-config

      - name: Install Wails CLI
        run: go install github.com/wailsapp/wails/v2/cmd/wails@v2.13.0

      - name: Install frontend deps
        run: cd frontend && pnpm install --frozen-lockfile

      - name: Stamp version
        shell: bash
        run: |
          sed -i.bak "s/^version: .*/version: ${{ inputs.version }}/" release.config.yaml

      - name: Build (macOS universal)
        if: matrix.platform == 'mac'
        run: make darwin

      - name: Build (Linux)
        if: matrix.platform == 'linux'
        # ubuntu-latest only ships libwebkit2gtk-4.1-dev now; this tag tells
        # wails/go to build against that instead of the removed 4.0 headers
        run: make build TAGS=webkit2_41

      - uses: actions/upload-artifact@v4
        if: matrix.platform == 'mac'
        with:
          name: bish-${{ inputs.version }}-mac
          path: build/bin

      - uses: actions/upload-artifact@v4
        if: matrix.platform == 'linux'
        with:
          name: bish-${{ inputs.version }}-linux
          path: build/bin/bish

  release:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Package artifacts
        run: |
          cd dist
          (cd bish-${{ inputs.version }}-mac && zip -r ../bish-${{ inputs.version }}-mac.zip bish.app)
          chmod +x bish-${{ inputs.version }}-linux/bish
          (cd bish-${{ inputs.version }}-linux && tar czf ../bish-${{ inputs.version }}-linux.tar.gz bish)

      - uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ inputs.version }}
          name: v${{ inputs.version }}
          files: |
            dist/*.zip
            dist/*.tar.gz
